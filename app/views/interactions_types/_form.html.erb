<%= form_for(@interactions_type) do |f| %>
  <% if @interactions_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interactions_type.errors.count, "error") %> prohibited this interactions_type from being saved:</h2>

      <ul>
      <% @interactions_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :icon %><br>
    <%= f.text_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
