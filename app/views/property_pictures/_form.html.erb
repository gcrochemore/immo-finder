<%= simple_form_for(@property_picture) do |f| %>
  <% if @property_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property_picture.errors.count, "error") %> prohibited this property_picture from being saved:</h2>

      <ul>
      <% @property_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :property %><br>
    <%= f.association :property %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
